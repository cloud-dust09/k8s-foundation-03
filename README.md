###Fully Qualified DNS name of a service is ###
```
<service-name>.<service-namespace>.svc.cluster.local
```

###Switch between namespaces###
```
kubectl config set-context --current --namespace=<insert-namespace-name-here>
```